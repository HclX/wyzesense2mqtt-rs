name: Build and Publish Docker Image

on:
  push:
    # Dev builds: every push to main
    branches: [ "main" ]
    # Release builds: version tags (pushed only on the 'release' branch)
    tags: [ "v*.*.*" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

# If a branch push and tag push arrive for the same commit (e.g. someone
# accidentally pushes a tag on main), cancel the earlier run so only the
# tag-triggered build survives.
concurrency:
  group: docker-${{ github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            # Tag pushes to main as 'dev'
            type=raw,value=dev,enable=${{ github.ref == 'refs/heads/main' }}
            # Tag PRs with pr-number
            type=ref,event=pr
            # Official Version tags (v1.2.3 -> 1.2.3, 1.2)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # Provide git SHA tag for all builds for exact tracing
            type=sha,format=short
            # Only mark actual git tags (releases) as 'latest'
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
